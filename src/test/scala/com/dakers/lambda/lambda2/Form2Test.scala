package com.dakers.lambda.lambda2

import com.dakers.lambda.{STNotation, UTNotation}
import org.scalatest.{FlatSpec, Matchers}

class Form2Test extends FlatSpec with Matchers with L2Notation with STNotation with UTNotation {
//  "An empty context of second order terms" should "not allow the form rule to apply" in {
//    Form2(Judgement(L2Context(), "x" :|| "X")) should be(None)
//  }
//
//  "A context containing one second order terms" should " allow the form rule to apply" in {
//    Form2(Judgement(L2Context(List("x" :|| "X")), "x" :|| "X")) should be(Some(Judgement(L2Context(List("x" :|| "X")), "x" :|| "X")))
//  }
}
